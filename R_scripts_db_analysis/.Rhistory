source("~/.staphopia_logon.R")
db <- staphopia_logon()
samples_tab <-tbl(db,"samples_sample")
samples_tab
select(samples_tab, is_published == TRUE)
select(samples_tab, is_published = TRUE)
select(samples_tab, is_published = 1)
select(samples_tab, is_published = 0)
select(samples_tab, is_published) %>% filter(TRUE)
select(samples_tab, is_published) %>% count()
select(samples_tab, is_published) %>% count(TRUE)
select(samples_tab, is_published) %>% count(is_published=TRUE)
count(samples_tab, is_published)
samples_tab
count(samples_tab, is_public)
count(samples_tab, vancomycin_mic)
count(samples_tab, isolation_country)
group_by(samples_tab, is_public)
count(samples_tab, sequencing_center)
count(samples_tab, sequencing_center) %>% top_n(20)
top_20_sc <- count(samples_tab, sequencing_center) %>% top_n(20)
top_20_sc
top_20_sc <- collect(count(samples_tab, sequencing_center) %>% top_n(20))
top_20_sc
as.data.frame(top_20_sc)
samples_tab
top_20_sc <- count(samples_tab, sequencing_center) %>% arrange
count(samples_tab,is_public)
as.data.frame(count(samples_tab,is_public))
as.data.frame(count(samples_tab,is_published))
samples_tab
as.data.frame(samples_tab,pubmed_id)
as.data.frame(select(samples_tab,pubmed_id))
top_20_sc <- count(samples_tab, sequencing_center) %>% top_n(20) %>% as.data.frame
top_20_sc
num_pub <- count(samples_tab,is_published) %>% as.data.frame
num_pub
barplot(top_20_sc)
plot(top_20_sc)
top_20_sc
str(top_20_sc)
samples_tab
db
